import { Movie, MovieCreate, MovieUpdate } from "./movies.interfaces";
import { Pagination, PaginationParams } from "./pagination.interfaces";

export {
    Movie,
    MovieCreate, 
    MovieUpdate,
    Pagination,
    PaginationParams,
};